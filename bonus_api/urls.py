from django.urls import path
from .views import *

urlpatterns = [
    # path("calculated-debt/", CalculateAPIView.as_view()),
    # path("system-errors/daily/",DailyApprovedCompensationDetailAPIView.as_view(),
    # ),
]